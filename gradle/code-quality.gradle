allprojects {
    apply plugin: "pmd"
}

pmd {
    toolVersion = "6.5.0"
    ignoreFailures = true
    consoleOutput = true
    ruleSetFiles = rootProject.files({ "/config/pmd/all-java.xml" })
    ruleSets = [
            'java-braces',
            'java-codesize',
    ]
}